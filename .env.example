# Firebase (Firestore) — Project settings > Service accounts > Generate new private key
FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Firebase Auth (client) — Project settings > General > Your apps > Web app config
NEXT_PUBLIC_FIREBASE_API_KEY="AIza..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"

# Session cookie signing (use a strong random secret, e.g. openssl rand -base64 32)
SESSION_SECRET="generate-a-strong-random-secret"
# Or reuse:
# NEXTAUTH_SECRET="generate-a-strong-random-secret"

# Only these emails can sign in (comma-separated). Leave empty to allow any Google account.
ALLOWED_EMAIL="your@gmail.com"

# OpenAI (for relevance scoring, cover letters, resume suggestions)
OPENAI_API_KEY="sk-..."

# Handshake cookie (paste from browser for Handshake scraper)
# HANDSHAKE_COOKIE="..."
